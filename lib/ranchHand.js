const Employee = require('./employee');

class ranchHand extends Employee {
    constructor(horseName, github) {
        super(fullName, role, email, stall)
        this.horseName = horseName;
        this.github = github;
    }
}

module.exports = ranchHand